# Agent container dependencies — pinned to an exact, mutually-compatible set
# resolved together (Python 3.12) for reproducible builds. Bump deliberately:
# re-resolve with `pip install` from the `>=` ranges, then re-pin to the freeze.

# Core web framework
fastapi==0.138.2
# Starlette is FastAPI's ASGI foundation. Pinned explicitly to >= 1.0.1 to
# resolve CVE-2026-48710 (Host-header auth bypass in Starlette < 1.0.1).
# 1.3.1 is proven compatible with fastapi 0.138.2 and bedrock-agentcore 1.15.1.
#
# CVE-2026-54283 (form-data parsing limits ignored for
# application/x-www-form-urlencoded -> DoS) affects 1.3.1, but no patched
# release exists yet (1.3.1 is the latest on PyPI as of this writing). This
# app does not parse urlencoded form data — /invocations reads the raw body
# and parses JSON, and there are no Form()/request.form() handlers — so the
# vulnerable code path is not reachable here. Bump when a fixed release ships.
starlette==1.3.1
uvicorn[standard]==0.49.0

# AWS SDK — bedrock-agentcore data-plane operations (ProcessPayment, etc.).
# Matches the shared Lambda layer pin.
boto3==1.43.37
botocore==1.43.37

# AgentCore SDK — Memory client + optional AgentCorePaymentsPlugin
bedrock-agentcore==1.15.1

# Strands Agents with Nova Sonic bidi support (voice mode) + OpenTelemetry
strands-agents[bidi,otel]==1.45.0

# HTTP client (storefront + seller x402 calls)
requests==2.34.2

# x402 Python SDK — PaymentPayload header formatting (EVM + SVM)
x402[svm]==2.14.0

# OpenTelemetry — ADOT auto-instrumentation for traces/metrics/logs
aws-opentelemetry-distro==0.18.0

# Environment
python-dotenv==1.2.2
